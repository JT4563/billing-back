# Server
PORT=4000
NODE_ENV=production

# Database
MONGO_URI=mongodb://127.0.0.1:27017/billing_system

# JWT Secret
JWT_SECRET=yourSuperSecretKey

# (Optional) Seed-only: access code for owner (one-time seed script reads this)
ACCESS_CODE_SEED=OWNER-123456
